<%= form_for(@seat, :remote => true) do |f| %>
  <% if @seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% @seat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :seatno %><br />
    <%= f.text_field :seatno %>
  </div>

  <div class="field">
    <%= f.label :nickname%>(Note: Don't need to type your nickname here if you've aleady logged in!)<br />
    <%= f.text_field :nickname %>
  </div>  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
